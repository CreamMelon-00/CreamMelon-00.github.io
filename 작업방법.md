# 혼자서 수정하는 법

## 가장 흔한 작업: 대사·연출 수정

1. `D:\StoryProject\script\` 안의 md 파일을 아무 텍스트 편집기로 수정
2. `D:\StoryProject\배포도구.bat` 더블클릭 → 반영할 파일 선택, 커밋 메시지 입력, [배포 실행]
3. 끝. (1~3분 뒤 사이트 반영)

script 폴더 안에 **챕터별 하위 폴더를 만들어 정리해도 된다** (예: `script\가문과 가족 1부\1편. ….md`).
배포 도구와 `--all`이 하위 폴더까지 전부 읽는다. 에피소드 매칭은 파일 위치가 아니라
제목 기준이라, 파일을 옮겨도 사이트의 진행도·데이터는 그대로 유지된다.

배포 도구에서 할 수 있는 것:
- 반영할 md 선택 (기본: 전체 선택) / [외부 md 추가…]로 script 폴더 밖 파일도 가능
- 커밋 메시지 직접 입력
- [미리 확인 (dry-run)] — 저장하지 않고 어떤 화가 어떻게 반영될지만 출력
- "푸시까지 실행" 체크를 끄면 커밋까지만

터미널이 편하면 `배포.bat`(전체 반영 + 커밋 + 푸시 원큐)도 그대로 쓸 수 있다.
대본 md는 저장소에 포함되므로 수정 이력이 git에 전부 남는다.
같은 제목의 에피소드는 갱신되고(진행도 유지), 새 제목이면 새 화로 추가된다.

## 대본 md 형식

```
7편. 그 날 무너진 것        ← 첫 줄: "N편. 제목" (N은 화 순서 기본값)
부제: 달리는 기차 위에...    ← 선택: 제목 바로 아래 메타 줄
커버: 수도주_의원실_아침     ← 선택: 로비 카드 표지 (에셋 id)
챕터: 가문과 가족            ← 선택: 없으면 기존 값 유지 (신규는 첫 챕터)

@배경 수도주_광장_심야
- 연방 수도주, 광장 | xx월 08일 : 심야

@정적 1.5

잭 | 연방 의원
대사...
```

연출 명령: `@배경 id` `@정적 1.5` `@레터박스 확대/기본` `@암전`/`@조명`
`@흔들림` `@비네트 약/강/끄기` `@카메라 확대/축소/기본` `@집중 id`
대사 마크업: `[고유명사]` `#크게 외치는 대사#` `*행동*`

## 프롤로그 / 에필로그

챕터마다 프롤로그·에필로그를 하나씩 둘 수 있다. md 예시:

```
1부 프롤로그              ← 제목은 챕터 구분용으로 유니크하게 (화면에는 안 나옴)
역할: 프롤로그            ← 이 줄이 핵심. "역할: 에필로그"도 동일
부제: 모든 것이 시작된 밤   ← 부제는 목록에 그대로 표시됨
챕터: 가문과 가족 1부
```

- 화면에는 번호 없이 "프롤로그"/"에필로그"로만 표기되고, 목록에서 항상 맨 앞/맨 뒤에 정렬된다
- **프롤로그는 챕터에 처음 들어갈 때 자동으로 재생**되고, 끝나면 화 목록으로 이어진다
- 화 번호(1화, 2화…)는 일반 편만 센다

## 연표 (사건 기록)

각 화의 md에 사건 한 줄을 적으면, **그 화를 끝까지 읽은 독자에게만** 연표에 표시된다.
(로비 상단의 "📜 연표" 메뉴 → 전용 화면. 아직 안 읽은 사건은 점선 카드로 자리만 보인다)

```
사건: 하노버 용병단, 스프링필드 저택을 습격해 보안관 레밍턴과 거래하다
날짜: 12일        ← 선택. 없으면 대본의 첫 씬 캡션(xx월 N일)에서 자동 추출
```

- 날짜는 "N일" 또는 "며칠 뒤" 같은 서술형도 가능 (서술형은 연표 맨 뒤에 배치)
- `사건:` 줄이 없는 화(프롤로그 등)는 연표에 나오지 않는다
- 사건 줄에는 그 화 안의 내용만 담을 것 — 읽은 사람에게 보이는 요약이므로
  그 화의 반전까지는 적어도 되지만, 이후 화의 내용은 적으면 안 된다

### 과거 사건 (본편에서 언급·회상되는 일)

한 화에 `과거사건:` 줄을 몇 개든 적을 수 있다. 연표의 **'이전 이야기'** 구간에 쌓이고,
그 화를 끝까지 읽어야 열리는 건 `사건:`과 똑같다.

```
과거사건: 3년 전 | 대가문을 등지고 철도청에 지원한 네빌을, 세라가 면접해 받아들이다 | 네빌
          └ 시점    └ 사건                                                          └ 주역(선택)
```

- **시점**은 대본에 쓰인 상대 표기를 그대로 쓴다 (`먼 과거` `3년 전` `몇 달 전` `두 달 전` `몇 주 전` …)
- 주역을 적으면 그 인물의 기호가 카드에 새겨진다. 생략하면 `·`
- 시점의 **배열 순서**는 `tools/inject_episode.py`의 `ERA_ORDER` 목록이 정한다.
  새 시점을 쓰면 자동 등록되지만 맨 뒤로 가므로, 순서를 맞추려면 이 목록에 끼워넣을 것

## 이해관계도 (수사보드)

로비의 "🧵 이해관계도" 메뉴. 세력별 구역에 인물 카드를 꽂고 관계를 실로 잇는다.
**연표와 달리 대본 md가 아니라 `script\_인물관계.md` 한 파일에 전부 적는다.**
(`_`로 시작하는 파일은 에피소드로 취급하지 않는다)

```
세력: 철도청 | 궤간 통일을 명분으로, 지하를 파는 조직
인물: 앤드루 | 철도청 | 철도청장 | 공개: 2편
관계: 앤드루 - 에른스트 | 고용 | 용병단을 앞세워 대가문을 '설득'시키다 | 공개: 2편
```

- `공개: N편`의 N은 대본 파일 이름의 편 번호(1~20). **그 편을 끝까지 읽어야 열린다**
- 관계는 **양쪽 인물이 모두 열린 뒤에야** 걸리므로, 관계의 공개 편은 두 인물보다 뒤여야 한다
- 아직 안 열린 인물은 점선 `???` 카드로 자리만 보이고, 관계선은 아예 그려지지 않는다
- 인물 카드를 누르면 그 사람에게 걸린 실만 남고 나머지는 흐려진다 (Esc로 해제)
- 선 모양은 라벨에서 자동으로 정해진다:
  혈연·형제·가족 → 굵은 선 / 적대·억압 → 갈라진 선 /
  밀약·포섭·감시·거래·이용 → 점선 / `?`로 시작 → 아주 흐린 점선 / 그 외 → 보통 선
- 기호(각인)가 등록된 인물은 카드에 기호가 함께 새겨진다

## 어록 (로비의 무작위 대사)

로비 첫 화면, 커버와 챕터 목록 사이에 본문 대사가 한 줄 뜬다.
`script\_어록.md` 한 파일에서 관리한다.

```
어록: 달리는 기차 위에 중립은 없다. | 2편
       └ 화면에 뜨는 대사        └ 출처메모(화면엔 안 나옴)
```

- **화자와 출처는 절대 표시되지 않는다.** 아직 안 읽은 사람을 끌어들이는 게 목적이라
  누가 언제 한 말인지 모를수록 좋다
- 그래서 고르는 기준이 하나 더 있다 — **스포일러가 되면 안 된다.**
  누가 죽거나, 정체가 밝혀지거나, 큰 사건의 결말이 드러나는 대사는 넣지 말 것
- 맥락 없이 읽어도 완결되는 문장이어야 한다 ("그래서 자네는?" 같은 건 안 됨)
- 길이는 15~55자 정도가 화면에 잘 맞는다
- 챕터 안으로 들어가면 숨고, 목록으로 돌아오면 다시 나온다
- 방문자가 누르면 다른 대사로 바뀐다 (연달아 같은 문장은 안 나온다)

## 주동인물 기호 (각인)

화 목록 오른쪽에 그 화의 주동인물을 암시하는 기호가 은은하게 새겨진다. 이름은 노출되지 않는다.

```
5편. 아버지의 그림자 下
주역: 세라              ← 이 화의 주동인물
기호: 📕                ← 처음 등장하는 인물이면 기호도 함께 (한 번 등록하면 생략 가능)
```

- 기호를 넣지 않을 화는 `주역:` 줄을 빼면 된다 (예: 6화 — 의도된 무기호)
- 등록된 매핑: 네빌 🎫 / 앤드루 🕰️ / 에른스트 🧨 / 레밍턴 🛡️ / 세라 📕 / 잭 📰 /
  데이비스 🎭 / 필립 🔦 / 로버트 ⛓️ / 하워드 ⚖️ / 해리엇 🎖️ / 타게니아 ♗ / 코네트 🗝️ / 헥터 ⚔️

## 배경 추가

1. 이미지를 아무 곳에 저장 (파일명 = "지역명_시간대.png" 권장, jpg/webp도 가능)
2. 터미널에서: `python tools\inject_episode.py --add-bg "이미지경로.png"`
   → resource/bg/ 에 **WebP로 자동 변환되어** 복사되고 에셋 id가 출력됨 (예: `북방주_술집_저녁`)
   (용량이 원본의 1/10 수준으로 줄어든다 — 사이트 로딩 속도에 직결되니 PNG를 그대로 두지 말 것)
3. 대본에서 `@배경 그id` 로 사용 → 배포.bat

## 새 에피소드 (PDF에서 이관)

1. `python tools\make_draft.py --source ex --pages 87-92 --title "11편. 개입"`
   → script 폴더에 초벌 md 생성 (`<<확인>>` 마커 지점만 손질)
2. 배경/연출 삽입 후 배포.bat

## 기타

- storypack.json은 **자동 생성 파일**이다. 직접 고치지 말고 md를 고쳐서 반영할 것.
  대본은 파일 안에 줄 단위 배열로 저장되므로, SourceTree 등에서 "어느 대사가 바뀌었는지"가
  그대로 보인다. 바뀐 내용이 없으면 배포 도구가 파일 자체를 건드리지 않는다.
- 현재 팩 상태 확인: `python tools\inject_episode.py --list`
- 실수했을 때: `data\storypack.json.bak`이 직전 백업. git 이력에도 전부 남아있음
- 챕터 이름/커버/순서 수정: data/storypack.json의 meta.chapters (또는 클로드에게 요청)
